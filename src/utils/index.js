/**
 * 计算平方
 * @param {Number} x 
 * @return {Number} 计算结果
 */

export const square = (x) => {
    // return x * x;
    console.log('y --->', y);
}

export const cube = (x) => {
    return x * x * x;
}